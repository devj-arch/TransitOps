Fleet Manager: Dashboard, Vehicle Registry (Full CRUD), Drivers (Read-only), Trips (Read-only), Maintenance (Full CRUD), Analytics (Fleet metrices only)

Dispatcher: Dashboard, Vehicle Registry (Read-only), Drivers (Read-only), Trips (Full-CRUD)

Safety Officer:  Dashboard, Vehicle Registry (Read-only), Drivers (Full CRUD), Trips (Read-only), Maintenance (Read-only), Analytics (Safety metrices only)

Financial Analyst: Dashboard, Vehicle Registry (Read-only), Trips (Read-only), Maintenance (Read-only), Fuel & Expenses (Full CRUD), Analytics (Full)

Admin: Should have access to all channels with an additional channel of “Settings”
